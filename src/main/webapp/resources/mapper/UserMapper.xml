<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
		PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	<mapper namespace="mapper.UserMapper">
	<insert id="userJoin">
		insert into EXCHANGERS_USER values(#{userId}, #{userPw}, #{userName}, #{userMail}, #{userZipCode}, #{userAddress}, #{userAddressDetail}, False, sysdate, 10)
	</insert>
</mapper>